<h1>New payments <%= @group %></h1>
<%= render "form", payment: @payment %>
<br>
<div>
  <% if params[:format] %>
    <%= link_to "Back to payments", group_path(params[:format]) %>
  <% else %>
    <%= link_to "Back to payments", groups_path() %>
  <% end%>
</div>
