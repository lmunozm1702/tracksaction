<%= form_with(model: @grouped_payment) do |form| %>
  <% if @grouped_payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@grouped_payment.errors.count, "error") %> prohibited this payment from being saved:</h2>
      <ul>
        <% @grouped_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
